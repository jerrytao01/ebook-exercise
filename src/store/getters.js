const book = {
  fileName: state => state.book.fileName,
  menuVisable: state => state.book.menuVisable,
  settingVisible: state => state.book.settingVisible,
  defaultFontSize: state => state.book.defaultFontSize
}

export default book
